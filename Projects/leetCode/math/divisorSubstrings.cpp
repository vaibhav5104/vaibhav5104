#include <iostream>
#include <math.h>
using namespace std;

int divisorSubstrings(int n,int k){
    /* vector<int> vec;

    // for(int i = 0; i<log10n;i++){
        // vec.push_back(str[i] - '0');
    // }

    int temp = n,count = 0;
    while(temp != 0){

        vec.push_back(temp%10);
        temp/=10;

    }

    for(int i = 0; i<vec.size();i++){
        int s= vec[i]*10 + vec[i+1];
        if(n % s == 0 && s != 0) count++;
    } */

    while(temp != 0){
        
    }


}

int main() {

    

    return 0;
}
